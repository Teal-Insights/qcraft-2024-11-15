[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.graph"

[deploy]
startCommand = "python scripts/serve_graph_api.py"
healthcheckPath = "/api/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
